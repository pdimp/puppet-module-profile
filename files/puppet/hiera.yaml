---
:backends:
  - yaml
:hierarchy:
  - "%{clientcert}"
  - "%{sp_serial_number}"
  - "%{role}"
  - "%{virtual}/%{operatingsystem}"
  - "%{operatingsystem}"
  - global

:yaml:
  :datadir: "/etc/puppet/hieradata/%{environment}"
